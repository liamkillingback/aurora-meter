started_utc=2026-09-15T13:00:39Z
[mixlane] core: DB_PORT=5490 elixir -S mix run --no-start /home/liamk/src/PhxTemplates/tmp/v1/06a-bench/bench.exs
running...
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
       coalesce(sum(available + reserved)
                FILTER (WHERE category = 'promotional'), 0)::bigint,
       coalesce(sum(expired), 0)::bigint
commit []
begin []
legacy     lots=    1 debit p50=3979us p95=4581us  hold p50=3938us  settle p50=4566us
allocator  lots=    1 debit p50=6069us p95=6599us  hold p50=6112us  settle p50=6829us
legacy     lots=   10 debit p50=4032us p95=4654us  hold p50=4026us  settle p50=4820us
allocator  lots=   10 debit p50=6218us p95=6873us  hold p50=6089us  settle p50=7241us
legacy     lots=  100 debit p50=4162us p95=4870us  hold p50=4097us  settle p50=5093us
allocator  lots=  100 debit p50=7361us p95=8285us  hold p50=6843us  settle p50=8289us
legacy     lots= 1000 debit p50=4229us p95=4733us  hold p50=4434us  settle p50=5396us
allocator  lots= 1000 debit p50=13215us p95=16130us  hold p50=13228us  settle p50=14858us
wrote /home/liamk/src/PhxTemplates/product-workspaces/aurora_meter/docs/evidence/v1/phase-06/06a-bench.json
[mixlane] core exit=0 started=2026-09-15T13:00:39Z finished=2026-09-15T13:01:27Z
exit=0 finished_utc=2026-09-15T13:01:27Z
